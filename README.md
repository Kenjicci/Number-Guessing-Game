# Number Guessing Game

 A simple game that lets user guess number until it is correct. The program ask the user an input for lower and upper bound. Then, it will generate a random number using python random module. The program loops until the user guess the correct number. Each guess, it will give hint if it is either 'Too high' or 'Too low'.
